<section class="py-5"></section>
<section class="py-4 bg-dark">

    <div class="container">
        <center>
            <%- include('layouts/msg')  %> 
            <%- include('layouts/errors')  %> 
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <p class="card-title text-center text-primary" style="font-size: 20px;">LOGIN FORM</p>
                        <p class="text-muted small-xl text-center">Killua Rest Api'S</p>
                        <form method="POST" action="/users/login">
                        <!-- <form id="forms"> -->
                            <div class="form-group">
                                <input style="font-family:Arial, FontAwesome" name="username" id="username" type="text" class="form-control" id="nameInput" placeholder="&#xf007;   Username" required>
                            </div>
                            <div class="form-group">
                                <input style="font-family:Arial, FontAwesome" name="password" id="password" type="password" class="form-control" id="exampleInputPassword1" placeholder="&#xf13e;   Password" required>
                            </div>
                           <div class="form-group">
                           <div
                            class="g-recaptcha"
                             data-sitekey="6LdpvDEUAAAAAMy8x0y8PS99j4BavfO2oBdVTQGZ"
                              ></div>
                              </div>
                            <div class="text-center">
                                <button type="submit" class="btn btn-primary mb-2"><i class="fas fa-sign-in-alt"></i><em class="ml-2"></em>Login</button>
                            </div>
                        </form>
                        <div class="text-center">
                            <p></p>
                            <p>Any Help ?</p>
                            <a class="btn btn-primary mb-2" href="/users/register"><i class="fas fa-clipboard-list"></i><em class="ml-2"></em>Register</a>    
                        </div>
                    </div>
                </div>
            </div>
        </center> 
    </div>
<script>
      document.getElementById('subscribeForm').addEventListener('submit', e => {
        e.preventDefault();

        const username = document.querySelector('#username').value;
        const password = document.querySelector('#password').value;
        const captcha = document.querySelector('#g-recaptcha-response').value;

        return fetch('/subscribe', {
          method: 'POST',
          headers: { 'Content-type': 'application/json' },
          body: JSON.stringify({ name, email, captcha })
        })
          .then(res => res.json())
          .then(data => {
            console.log(data);
            alert(data.msg);
            if (data.success) return location.reload();
          });
      });
    </script>
</section>
